import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class Shrey extends StatelessWidget {
  const Shrey({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Shrey'),
      ),
      body: Container(
        child: Column(
          children: [
            // Add your portfolio information widgets here
          ],
        ),
      ),
    );
  }
}